/**
 * Component 组件基类
 * 组件是纯数据的容器，不包含任何游戏逻辑
 * 
 * 什么是组件？
 * 就像给角色添加不同的"标签"或"属性"
 * 比如：位置组件记录角色在哪里，渲染组件记录角色长什么样
 */
export class Component {
  constructor() {
    // 组件所属的实体ID
    this.entityId = null;
  }
  
  /**
   * 初始化组件
   * 当组件被添加到实体时调用
   */
  init() {
    // 子类可以重写这个方法来初始化数据
  }
  
  /**
   * 销毁组件
   * 当组件从实体移除时调用
   */
  destroy() {
    // 子类可以重写这个方法来清理资源
  }
}
